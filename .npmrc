# pnpm 配置
# 自动安装 peer dependencies
auto-install-peers=true

# 使用 hoisted 模式（与 npm 类似，避免 Obsidian 插件的模块解析问题）
shamefully-hoist=true

# 严格模式（推荐）
strict-peer-dependencies=false

# 保存 exact 版本
save-exact=true

# 允许 esbuild 运行构建脚本
onlyBuiltDependencies=esbuild
